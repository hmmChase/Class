import React from 'react';
// import PropTypes from 'prop-types';
import * as sc from './Discord.style';

const Discord = props => (
  <sc.Container>
    <h1>Discord</h1>
  </sc.Container>
);

// Discord.propTypes = {
//   // myProp: PropTypes.string.isRequired
// };

export default React.memo(Discord);

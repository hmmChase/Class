# Migration `20200731131842`

This migration has been generated by Chase at 7/31/2020, 1:18:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Role" AS ENUM ('TEACHER', 'STUDENT');

CREATE TABLE "public"."User" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"email" text  NOT NULL ,
"username" text  NOT NULL ,
"password" text   ,
"name" text   ,
"role" "Role" NOT NULL DEFAULT E'STUDENT',
"avatarUrl" text   ,
"resetPassToken" text   ,
"resetPassTokenExpiry" timestamp(3)   ,
"hasDiscordLogin" boolean  NOT NULL DEFAULT false,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Question" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"authorId" integer  NOT NULL ,
"body" text  NOT NULL ,
"isAnswer" boolean  NOT NULL DEFAULT false,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Question" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200731131842
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,47 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+// Do this whenever this files changes:
+// npx prisma migrate save --experimental
+// npx prisma migrate up --experimental
+// npx prisma generate
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id                   Int        @id @default(autoincrement())
+  createdAt            DateTime   @default(now())
+  updatedAt            DateTime   @updatedAt
+  email                String     @unique
+  username             String
+  password             String?
+  name                 String?
+  role                 Role       @default(STUDENT)
+  avatarUrl            String?
+  questions            Question[]
+  resetPassToken       String?
+  resetPassTokenExpiry DateTime?
+  hasDiscordLogin      Boolean    @default(false)
+}
+
+model Question {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  author    User     @relation(fields: [authorId], references: [id])
+  authorId  Int
+  body      String
+  isAnswer  Boolean  @default(false)
+}
+
+enum Role {
+  TEACHER
+  STUDENT
+}
```



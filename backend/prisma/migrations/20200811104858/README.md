# Migration `20200811104858`

This migration has been generated by Chase at 8/11/2020, 10:48:58 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Role" AS ENUM ('TEACHER', 'STUDENT');

CREATE TABLE "public"."User" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"hasDiscordLogin" boolean  NOT NULL DEFAULT false,
"role" "Role" NOT NULL DEFAULT E'STUDENT',
"email" text  NOT NULL ,
"username" text  NOT NULL ,
"password" text   ,
"name" text   ,
"avatarUrl" text   ,
"resetPassToken" text   ,
"resetPassTokenExpiry" timestamp(3)   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Question" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"body" text  NOT NULL ,
"user_id" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Comment" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"body" text  NOT NULL ,
"question_id" integer  NOT NULL ,
"user_id" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.resetPassToken" ON "public"."User"("resetPassToken")

ALTER TABLE "public"."Question" ADD FOREIGN KEY ("user_id")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Comment" ADD FOREIGN KEY ("question_id")REFERENCES "public"."Question"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Comment" ADD FOREIGN KEY ("user_id")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200811104858
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,59 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+// Do this whenever this files changes:
+// npx prisma migrate save --experimental
+// npx prisma migrate up --experimental
+// npx prisma generate
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id                   Int        @id @default(autoincrement())
+  createdAt            DateTime   @default(now())
+  updatedAt            DateTime   @updatedAt
+  hasDiscordLogin      Boolean    @default(false)
+  role                 Role       @default(STUDENT)
+  email                String     @unique
+  username             String
+  password             String?
+  name                 String?
+  avatarUrl            String?
+  resetPassToken       String?    @unique
+  resetPassTokenExpiry DateTime?
+  questions            Question[]
+  comments             Comment[]
+}
+
+model Question {
+  id        Int       @id @default(autoincrement())
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  body      String
+  comments  Comment[]
+  author    User      @relation(fields: [user_id], references: [id])
+  user_id   Int
+}
+
+model Comment {
+  id          Int      @id @default(autoincrement())
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt
+  body        String
+  question    Question @relation(fields: [question_id], references: [id])
+  question_id Int
+  author      User     @relation(fields: [user_id], references: [id])
+  user_id     Int
+}
+
+enum Role {
+  TEACHER
+  STUDENT
+}
```



# Migration `20200803105542`

This migration has been generated by Chase at 8/3/2020, 10:55:42 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Role" AS ENUM ('TEACHER', 'STUDENT');

CREATE TABLE "public"."User" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"hasDiscordLogin" boolean  NOT NULL DEFAULT false,
"role" "Role" NOT NULL DEFAULT E'STUDENT',
"email" text  NOT NULL ,
"username" text  NOT NULL ,
"password" text   ,
"name" text   ,
"avatarUrl" text   ,
"resetPassToken" text   ,
"resetPassTokenExpiry" timestamp(3)   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Question" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"body" text  NOT NULL ,
"authorId" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Reply" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL ,
"body" text  NOT NULL ,
"questionId" integer  NOT NULL ,
"authorId" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Question" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Reply" ADD FOREIGN KEY ("questionId")REFERENCES "public"."Question"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Reply" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200803105542
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,59 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+// Do this whenever this files changes:
+// npx prisma migrate save --experimental
+// npx prisma migrate up --experimental
+// npx prisma generate
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id                   Int        @id @default(autoincrement())
+  createdAt            DateTime   @default(now())
+  updatedAt            DateTime   @updatedAt
+  hasDiscordLogin      Boolean    @default(false)
+  role                 Role       @default(STUDENT)
+  email                String     @unique
+  username             String
+  password             String?
+  name                 String?
+  avatarUrl            String?
+  resetPassToken       String?
+  resetPassTokenExpiry DateTime?
+  questions            Question[]
+  replies              Reply[]
+}
+
+model Question {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  body      String
+  replies   Reply[]
+  author    User     @relation(fields: [authorId], references: [id])
+  authorId  Int
+}
+
+model Reply {
+  id         Int      @id @default(autoincrement())
+  createdAt  DateTime @default(now())
+  updatedAt  DateTime @updatedAt
+  body       String
+  question   Question @relation(fields: [questionId], references: [id])
+  questionId Int
+  author     User     @relation(fields: [authorId], references: [id])
+  authorId   Int
+}
+
+enum Role {
+  TEACHER
+  STUDENT
+}
```


